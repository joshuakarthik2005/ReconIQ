# Dev/test/lint tooling -- not needed to run the pipeline itself, only to
# develop it. Keep separate from requirements.txt (runtime deps only).
pytest>=8.0.0
ruff>=0.15.0
mypy>=1.10.0
hypothesis>=6.100.0
